services:
  mongo:
    image: mongo:7
    container_name: pinpoint-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - pinpoint_mongo_data:/data/db

volumes:
  pinpoint_mongo_data:

